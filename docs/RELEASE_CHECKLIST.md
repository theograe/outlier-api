# Release Checklist

- confirm no real keys are committed
- confirm `.env` stays untracked
- run `npm run lint`
- run `npm run build`
- run `npm run test`
- verify a fresh project can:
- add channels
- run a scan
- search outliers
- save references
- verify README, API docs, CLI docs, and MCP docs match the shipped product
